/**
 * UI Configuration
 * Ten plik zawiera konfigurację elementów interfejsu użytkownika.
 */

// Informacje o aplikacji
export const APP_INFO = {
  // Wersja aplikacji wyświetlana w interfejsie
  VERSION: 'v1.0.1',
  // Nazwa aplikacji
  NAME: 'Atlas Code-Gen',
};

// Klucze używane do przechowywania danych w localStorage
export const STORAGE_KEYS = {
  // Klucz do przechowywania ustawienia dark mode
  DARK_MODE: 'dark-mode',
  // Klucz do przechowywania statusu certyfikatów
  CERT_STATUS: 'cert_status',
};

// Identyfikatory elementów HTML używane do manipulacji DOM
export const DOM_IDS = {
  // ID przycisku przełączającego tryb ciemny
  DARK_MODE_TOGGLE: 'high-contrast-toggle',
};

// Klasy CSS
export const CSS_CLASSES = {
  // Klasa używana do aktywacji trybu ciemnego
  DARK_MODE: 'dark-mode',
};

// Konfiguracja motywów kolorystycznych
export const THEME = {
  // Kolory ikon
  ICON_COLORS: {
    PRIMARY: 'text-blue-600',
  },
  // Rozmiary ikon
  ICON_SIZES: {
    SMALL: 16,
    MEDIUM: 28,
  },
}; 